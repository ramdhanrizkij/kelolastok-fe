import { PagePlaceholder } from "@/components/page-placeholder"

export default function SupplierReturnsPage() {
  return (
    <PagePlaceholder
      title="Supplier Returns"
      description="Pengembalian barang rusak (defect) atau tidak sesuai spesifikasi kembali ke pemasok / supplier."
      section="Inbound & Outbound"
      subGroup="Inbound (Barang Masuk)"
    />
  )
}

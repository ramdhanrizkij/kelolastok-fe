import { PagePlaceholder } from "@/shared/components/feedback/page-placeholder"

export default function ReorderAutomationPage() {
  return (
    <PagePlaceholder
      title="Reorder Automation"
      description="Aturan otomatisasi pemesanan ulang stok (Reorder Point / ROP), Safety Stock, dan pembuatan Purchase Order otomatis."
      section="Settings"
      subGroup="System Configuration"
    />
  )
}

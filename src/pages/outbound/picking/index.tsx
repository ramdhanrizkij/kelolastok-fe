import { PagePlaceholder } from "@/components/page-placeholder"

export default function PickingPackingPage() {
  return (
    <PagePlaceholder
      title="Picking & Packing"
      description="Proses pengambilan barang dari rak penyimpanan (picking) dan pengemasan pesanan (packing)."
      section="Inbound & Outbound"
      subGroup="Outbound (Barang Keluar)"
    />
  )
}
